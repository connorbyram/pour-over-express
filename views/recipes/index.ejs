<%- include('../partials/header') %>

<div class="main">
    <section>
        <div class="container">
            <div>
                <% recipes.forEach(function(recipe) { %>
                    <% if(recipe.public) { %>
                        <div id="recipe-preview" class="main-div">
                            <div id="user-header">
                                <img alt="avatar" id="avatar" src="<%= recipe.user.avatar %>" referrerpolicy="no-referrer">
                                <div class="title-data">
                                    <h3><%= recipe.beanVarietal %></h3>
                                    <h1><%= recipe.title %></h1>
                                    <a href="/users/<%= recipe.user._id %>"><h4><%= recipe.user.name %> | <%= recipe.createdAt.toLocaleDateString() %></h4></a>
                                </div>
                            </div>
                            <div class="recipe-data">
                                <strong><%= recipe.roaster %></strong> 
                                <strong><%= recipe.processMethod %></strong>
                                <strong><%= recipe.roastLevel %></strong>
                            </div>
                            <a href="/recipes/<%= recipe._id %>" class="button-main">
                                DETAILS <img src="/assets/images/arrow-white.svg" class="arrow">
                            </a>
                        </div>
                    <% } %>
                <% }); %>
            </div>
        </div>
    </section>
</div>


<%- include('../partials/footer') %>